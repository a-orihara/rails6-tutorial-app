<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "sample app", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "Help", help_path %></li>

        <%# !current_user.nil? %>
        <% if logged_in? %>
          <%# users_path:/users %>
          <li><%= link_to "Users", users_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <%# 1 current_user:@current_userを返す%>
              <li><%= link_to "Profile", current_user %></li>
              <%# edit_user_path(current_user):/users/#{current_user}/edit%>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%# 2%>
                <%= link_to "Log out", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>

      </ul>
    </nav>
  </div>
</header>

<%# 1
< link_to "Profile", user_path(current_user) >の省略形。
current_user = @current_user = モデルオブジェクトだから、user_path(current_user)に
Railsによって変換される。
%>
<%# 2
ハッシュ[method: :delete]では、HTTP の DELETE リクエストを使うように指示
%>